import React from 'react';

const Header = () => {
	return(
		<div className="container-fluid" style={{ height: 75, backgroundColor: 'dodgerblue' }}>
			<nav>HEADER</nav>
		</div>
	);
};

export default Header;